# Verify E2E on Linux
#
# Runs end-to-end verification of the local `setup-astm` action implementation
# on Linux runners.

name: Verify E2E on Linux

on:
  push:
    branches: [ "main" ]
    paths-ignore:
      - '**.md'
  pull_request:
    branches: [ "main" ]
    paths-ignore:
      - '**.md'
  workflow_dispatch:
    inputs:
      release_version:
        description: Semver release version of astm to install
        required: true
        default: 23.2.0
        type: string

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.head.ref || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  release-linux:
    name: Release Install on Linux
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5

      - name: Setup Development Environment
        uses: ./.github/actions/setup

      - name: Build Action Distribution
        run: npm run package

      - name: Run setup-astm in Release Mode
        id: setup_astm
        uses: ./
        with:
          token: ${{ secrets.ASTM_RELEASE_PAT }}
          version: ${{ inputs.release_version || '23.2.0' }}

      - name: Verify Action Outputs
        run: |
          test "${{ steps.setup_astm.outputs.version-ref }}" = "${{ inputs.release_version || '23.2.0' }}"
          test "${{ steps.setup_astm.outputs.install-mode }}" = "release-tag"

      - name: Verify Installed astm Binary
        run: |
          command -v astm
          astm --version

  main-linux:
    name: Main Install on Linux
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5

      - name: Setup Development Environment
        uses: ./.github/actions/setup

      - name: Build Action Distribution
        run: npm run package

      - name: Setup Rust Toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Run setup-astm in Main Mode
        id: setup_astm
        uses: ./
        with:
          token: ${{ secrets.ASTM_RELEASE_PAT }}
          version: main
          submodule_token: ${{ secrets.ASTM_SUBMODULE_PAT }}

      - name: Verify Action Outputs
        run: |
          test "${{ steps.setup_astm.outputs.version-ref }}" = "main"
          test "${{ steps.setup_astm.outputs.install-mode }}" = "main"

      - name: Verify Installed astm Binary
        run: |
          command -v astm
          astm --version
